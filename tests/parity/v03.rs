//! Stub for the V03 value-block parity mirror (issue #72). Activated as
//! part of that block's exit gate, same drop-order convention as
//! `.openspec/plan.md`'s Value Blocks table. See `v01.rs` for the
//! implemented pattern this will follow once V03 lands.

#[test]
#[ignore = "V03 not yet implemented (#72)"]
fn parity_pending() {
    unreachable!("stub — activate once V03 lands");
}
